# PostgreSQL Configuration
# Copy this file to .env.local and adjust values as needed

# Database connection (matches docker-compose.yml defaults)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=travel_agent
POSTGRES_USER=travel_user
POSTGRES_PASSWORD=travel_secret_2024

# ===========================================
# Map Provider (Optional)
# ===========================================
# By default, the app uses OpenStreetMap + Leaflet (free, no signup).
# For premium Mapbox GL JS maps, add your token below.
#
# Get a free token at: https://account.mapbox.com/
# Free tier includes 50,000 map loads/month
#
# NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token_here
#
# When token is present:
# - Uses Mapbox GL JS with vector tiles (smoother, faster)
# - Better 3D buildings, terrain, and styling
# - Default style: streets-v12
#
# When token is missing:
# - Falls back to OpenStreetMap + Leaflet (free)
# - Console warning shown in development mode only

# ===========================================
# Optional APIs
# ===========================================
# OpenTripMap API key (for attraction enrichment)
# Get one free at: https://opentripmap.io/product
# OPENTRIPMAP_API_KEY=your_api_key_here

# Feature flags
# ENABLE_OPENTRIPMAP_ENRICHMENT=false
